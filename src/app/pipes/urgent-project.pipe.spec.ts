import { UrgentProjectPipe } from './urgent-project.pipe';

describe('UrgentProjectPipe', () => {
  it('create an instance', () => {
    const pipe = new UrgentProjectPipe();
    expect(pipe).toBeTruthy();
  });
});
